App Main

Cliente:
 
npm install (instalar dependencias angular), 
ng serve (arrancar aplicacion) port 4200 (por defecto)

App BackOffice

Cliente:

npm install (instalar dependencias angular),
ng serve (arrancar aplicacion) port 4201 (por defecto)

Back -> CRUD no funcional, solo delete
